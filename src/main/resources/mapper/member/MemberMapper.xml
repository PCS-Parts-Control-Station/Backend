<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pcs.domain.member.mapper.MemberMapper">

    <insert id="insert" parameterType="com.pcs.domain.member.entity.Member"
            useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO tb_member (
            company_id,
            login_id,
            password_hash,
            name,
            role,
            owner_slot,
            password_status,
            temp_password_expires_at,
            active,
            created_by
        ) VALUES (
            #{companyId},
            #{loginId},
            #{passwordHash},
            #{name},
            #{role},
            #{ownerSlot},
            #{passwordStatus},
            #{tempPasswordExpiresAt},
            #{active},
            #{createdBy}
        )
    </insert>

</mapper>
